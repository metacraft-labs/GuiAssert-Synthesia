# Nim build artefacts.
nimcache/
nimblecache/
*.exe
*.dSYM
tests/tsynthesia

# Editor / OS noise.
.DS_Store
*.swp
*.bak

# Nix results.
/result
/result-*

# Auto-generated by git-hooks.nix in the workspace's nix devShell.
/.pre-commit-config.yaml

# Local scratch — never commit credentials or render outputs.
/.env
/.env.*
/tmp/

# User-local direnv overrides (e.g. age-secrets loader for API keys).
/user.envrc
/.direnv/
tests/temotive_translation
