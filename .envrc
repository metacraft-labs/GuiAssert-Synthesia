# shellcheck shell=bash
# Public direnv config for this plugin repo.
# User-local overrides (e.g. API keys) belong in `user.envrc`, which is
# gitignored and sourced below if present.

use flake

if [[ -f user.envrc ]]; then
    source_env user.envrc
fi
